/* Tunables (extracted) */
export const BATCH_MS = 100;           // batch flush rate (~10 fps)
export const TRAIL_MAX = 60;           // keep trails short
export const MAX_TRAILS_DRONES = 120;  // store trails only for selected + first N
export const GAP_MS = 15_000;          // new flight if long gap
export const DEFAULT_URL = "https://sagerassessmentserver.onrender.com";
